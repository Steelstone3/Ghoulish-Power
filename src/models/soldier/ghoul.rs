#[allow(dead_code)]
pub struct Ghoul {}

impl Ghoul {
    #[allow(dead_code)]
    pub fn new() -> Self {
        todo!()
    }
}

#[cfg(test)]
mod model_should {
    use rstest::rstest;
    // use super::*;

    #[ignore = "need to implement random system"]
    #[rstest]
    #[case()]
    fn create_new_ghoul() {
        // Given

        // When

        // Then
    }
}
